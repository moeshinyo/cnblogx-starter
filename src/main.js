// TIP: 导出一个`main`函数：该函数将在页面加载时被调用。

export function main() {
    // TIP: 在这里添加代码。
    
    // 示例代码：打印`main.ejs`中定义的“你好，世界。”。
    console.log(document.getElementById('custom-hello-world').innerText.trim());
}
